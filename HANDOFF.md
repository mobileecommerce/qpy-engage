# Qpy Engage engineering handoff

Updated: July 18, 2026 (real authentication, per-workspace isolation, and a real AI assistant provider are all live in production now — read "Authentication" and "AI assistant" below before assuming this is still a demo-only app)

## Source and deployments

- Repository: https://github.com/mobileecommerce/qpy-engage
- Continue from branch: `agent/publish-github-pages`
- Branch URL: https://github.com/mobileecommerce/qpy-engage/tree/agent/publish-github-pages
- Live frontend: https://mobileecommerce.github.io/qpy-engage/
- GitHub Pages source branch: `gh-pages`
- Cloudflare Worker: `qpy-engage-api`
- Worker URL: https://qpy-engage-api.qpy-engage.workers.dev
- D1 database: `qpy-engage-db`

Do not continue from `main`; it is behind the active branch.

## Product state

### Working with a real backend

- Real user accounts, sessions, and per-workspace data isolation (see "Authentication and multi-tenancy")
- WhatsApp Cloud API manual connection using WABA ID, Phone Number ID, and a Meta access token
- Server-side validation of Meta assets
- AES-GCM encrypted access-token storage in Cloudflare D1
- WABA webhook subscription
- Signed WhatsApp webhook verification and ingestion
- Live Inbox loading from D1 with eight-second refresh
- Live inbound WhatsApp messages
- Outbound free-form WhatsApp replies through Graph API during the customer-service window
- Meta `hello_world` test message
- Connection diagnostics and token-refresh recovery
- Public privacy, terms, and data-deletion pages
- Real AI assistant replies (text and voice) through Anthropic Claude — see "AI assistant" below

### Implemented primarily as browser-product functionality

- AI assistant builder UI: instructions, actions, voice configuration, governance, and publishing steps are all real inputs, but there's no real knowledge retrieval (see "AI assistant" below) and AI actions (webhooks) aren't actually called yet
- Campaign builder, audience import, image attachment, scheduling, and campaign records
- Automation builder and templates
- Team and subscription management UI (team membership is real — see auth section; subscription/billing is not)
- Analytics and exports
- Web-chat widget setup

These product areas persist per-workspace through `/api/state` now that real accounts exist (see below), so they sync across devices/browsers for the same signed-in account, but most are still interactive simulations, not calls to production services. Instagram is currently a sandbox/demo connection. Campaign "sends" and billing are simulated; no bulk WhatsApp/Instagram broadcast API or payment processor is wired up.

## AI assistant

Real text and voice replies were added on top of the existing Assistant builder UI, via `worker/assistant.ts`:

- `POST /api/assistant/respond` (session-authenticated) sends a system prompt + conversation history to Anthropic's Messages API (`claude-sonnet-5`) and returns the reply.
- The frontend composes the system prompt from the assistant's actual configured role/instructions, tone, fallback/handoff policy, restricted topics, and the *names* of connected knowledge sources — not their content. **There is no real retrieval/RAG**: no document content is ever extracted or indexed anywhere in this codebase; "Knowledge sources" are just metadata (name/type/page count). The prompt explicitly tells the model not to invent specific facts attributed to sources it wasn't actually given, but it also can't answer from real business content until real ingestion is built.
- Test Studio → Chat simulation calls this for real replies.
- Test Studio → Voice call now does a genuine listen → think → speak loop entirely in the browser: the Web Speech API's `SpeechRecognition` (Chrome/Edge only — not supported in Safari/Firefox) transcribes what the tester says, sends it through the same assistant endpoint, and speaks the reply with `SpeechSynthesis`, with a live transcript shown during the call.
- **Requires `ANTHROPIC_API_KEY` as a Cloudflare Worker secret** (`npx wrangler secret put ANTHROPIC_API_KEY`) to actually generate replies; without it the endpoint returns a clear 503 "not configured" error rather than failing silently or faking a response.
- AI actions (the webhook-calling "AI actions" builder) are still not actually invoked by real conversations — `testAction()` still just marks a canned "Passed" without calling the configured endpoint.
- **Real inbound phone calls are a separate, much bigger project**: they need a telephony provider account (e.g. Twilio) that you create yourself, a real phone number per workspace (provisioned via that provider's API), and webhook-driven call handling — none of that exists yet. The realistic path for customers to use their *existing* business number is call forwarding (conditional or always) to a number provisioned through that provider, not porting.

## Authentication and multi-tenancy

Real accounts replaced the single shared `default` workspace and the `x-qpy-setup-key` model:

- `worker/auth.ts` implements signup/login/logout, PBKDF2 password hashing, and bearer session tokens (`sessions` table, 30-day expiry). No new secret is required — session tokens are self-contained.
- New D1 tables: `users`, `workspaces`, `workspace_members`, `sessions`. `whatsapp_messages` gained a `workspace_id` column. All are created idempotently at request time (see `ensureAuthSchema` / `ensureMetaSchema`) the same way the existing WhatsApp tables are, so no manual migration step is required to deploy this.
- Every `/api/meta/*` endpoint (except `config`, which is server-level non-tenant config) now requires a valid `Authorization: Bearer <token>` session instead of the old shared setup key. Connecting/disconnecting WhatsApp requires the `Owner` or `Admin` role.
- `/api/state` is now namespaced per authenticated workspace (`${workspaceId}::${key}` internally) instead of being a single global keyspace anyone could read or write.
- **First-signup migration**: when the very first user account is created, if a legacy `whatsapp_connections` row still exists under the old hardcoded workspace id `"default"`, that workspace id (and any `whatsapp_messages` rows with a null `workspace_id`) are adopted by that user's new workspace, so the already-connected WhatsApp number keeps working under the first real account. Every signup after that gets its own fresh workspace.
- The frontend now gates the whole app behind login/signup (`app/page.tsx`'s `AuthGate`/`Home`/`Workspace` split). The bearer token lives in `localStorage` (`qpy-engage-auth-token`) and is attached via an `AuthTokenContext` used by `useStoredState`, `Channels`, `LiveInbox`, and the new `useWorkspaceMembers` hook (Team page — invite/role/remove now hit real `/api/workspace/members` endpoints instead of local mock state).
- Role gating so far is minimal: Team invite/role-change/remove and WhatsApp connect/disconnect require Owner/Admin (enforced server-side); other pages don't yet differentiate by role.

**Verified**: Node.js was set up in the building session (there wasn't any initially), `npm run build` / `npm run build:pages` / `npx tsc --noEmit` all pass, and this has been deployed to production and click-tested end to end (signup, WhatsApp connection carrying over, live Inbox). One migration-ordering bug was found and fixed in production (see commit history) — a first-signup's legacy-workspace-claim could run before the `whatsapp_messages.workspace_id` column existed; `auth.ts` now adds that column itself before using it, so it's no longer order-dependent.

**Not yet done**: there is no "forgot password," email verification, or workspace-switching UI (a user invited to a second workspace only gets attached to it in the database, with login always resolving to their `Owner` workspace first). The account that currently owns the live "default" workspace is a smoke-test account created during deploy verification (`smoketest+deploycheck@example.com`) — the real user chose to keep using it rather than have it cleaned up, so there's no admin UI yet to rename a workspace or change an account's email/password.

## Current WhatsApp/Meta status

- Meta App ID: `966180749793118` (not a secret)
- Embedded Signup configuration ID: `2294788824594343` (not a secret)
- Graph API version: `v25.0`
- Callback URL: https://qpy-engage-api.qpy-engage.workers.dev/api/webhooks/whatsapp
- A Meta test WABA and test phone number have been connected successfully.
- Real inbound message delivery into Qpy Engage has been confirmed.
- The most recent outbound reply failed because the temporary Meta user access token became invalid.
- The user must replace it with a Meta System User Access Token, ideally with `Never` expiration, and these permissions:
  - `whatsapp_business_management`
  - `whatsapp_business_messaging`
  - `business_management` when required for business-portfolio operations
- Qpy Engage provides Channels → WhatsApp → Refresh access token for this update.
- Business Verification and App Review are intentionally postponed.
- Embedded Signup is configured but cannot be treated as production-ready until Meta verification, App Review, and Advanced Access are completed.
- The Meta app is unpublished, so real production webhook delivery can be restricted by Meta. Dashboard test webhooks remain useful during development.

## Critical security notes

Never commit or print any of these values:

- `META_APP_SECRET`
- `META_TOKEN_ENCRYPTION_KEY`
- `META_WEBHOOK_VERIFY_TOKEN`
- Meta user or system-user access tokens
- User password hashes / session token hashes (not secrets you set, but never log them)

They are stored as Cloudflare Worker secrets. The Meta App Secret was shared in a prior private development conversation and should still be rotated before production; update the Cloudflare secret immediately after rotating so webhook signature validation and `appsecret_proof` continue to work. `META_SETUP_KEY` is retired — it's no longer read anywhere in the code and can be deleted from Cloudflare secrets.

Expected Cloudflare values:

- Non-secret vars in `wrangler.jsonc`: `META_APP_ID`, `META_EMBEDDED_SIGNUP_CONFIG_ID`, `META_GRAPH_VERSION`
- Secrets: `META_APP_SECRET`, `META_TOKEN_ENCRYPTION_KEY`, `META_WEBHOOK_VERIFY_TOKEN`

## Backend API map

- `GET /api/meta/config`: public non-secret Meta configuration
- `GET /api/meta/status`: sanitized connection status for the caller's workspace; requires a session
- `POST /api/meta/oauth/exchange`: Embedded Signup code exchange; requires an Owner/Admin session
- `POST /api/meta/manual/connect`: manual/system-user token connection; requires an Owner/Admin session
- `GET /api/meta/inbox`: live stored messages for the caller's workspace; requires a session
- `POST /api/meta/inbox/messages`: send live WhatsApp reply; requires a session
- `POST /api/meta/test-message`: send approved test template; requires a session
- `DELETE /api/meta/connection`: disconnect; requires an Owner/Admin session
- `GET|POST /api/webhooks/whatsapp`: Meta challenge and signed webhook delivery (unauthenticated by design — Meta calls this directly; resolves the workspace from the incoming `phone_number_id`)
- `POST /api/auth/signup`, `POST /api/auth/login`, `POST /api/auth/logout`, `GET /api/auth/session`: account auth
- `GET /api/workspace/members`, `POST /api/workspace/members` (invite), `PATCH /api/workspace/members/:email` (role), `DELETE /api/workspace/members/:email`: team management, Owner/Admin only for writes
- `POST /api/assistant/respond`: real AI assistant reply via Anthropic Claude; requires a session and `ANTHROPIC_API_KEY`

The GitHub frontend calls the Worker through `META_BACKEND_ORIGIN` in `app/page.tsx`. CORS currently allows `https://mobileecommerce.github.io`, local development, and ChatGPT Sites preview hosts.

## Deployment

### Frontend / GitHub Pages

1. Validate the static build:

   ```bash
   NEXT_PUBLIC_BASE_PATH=/qpy-engage npm run build:pages
   ```

2. Publish the generated `out/` directory to the `gh-pages` branch.
3. Confirm https://mobileecommerce.github.io/qpy-engage/ serves the new build.

The product source remains on `agent/publish-github-pages`; do not edit generated GitHub Pages files as product source.

### Cloudflare Worker

1. Validate:

   ```bash
   npm run build
   ```

2. Deploy using the production configuration:

   ```bash
   npx wrangler deploy --config wrangler.jsonc
   ```

vinext may generate `.wrangler/deploy/config.json` with a duplicate local `DB` binding. If Wrangler reports that `DB` is assigned twice, temporarily move that generated redirect file out of `.wrangler/deploy/`, deploy with `wrangler.jsonc`, and restore it afterward. Do not alter the production D1 binding in `wrangler.jsonc`.

**Do not skip step 1.** `wrangler.jsonc`'s `main` points at `dist/server/index.js` — a bundled build artifact, not the `worker/*.ts` source directly. `npx wrangler deploy` will happily deploy whatever is currently sitting in `dist/`, even if it predates your latest edits — `wrangler deploy` reports "success" either way, so a stale deploy is silent, not an error. `npx tsc --noEmit` on its own only type-checks; it does **not** regenerate `dist/`. A real incident on 2026-07-19: several worker/*.ts fixes in a row were deployed using only `tsc --noEmit` before `wrangler deploy`, so `dist/server/index.js` silently went stale — the fixes were never actually live for a while, even though each deploy "succeeded" and initial verification looked plausible (the underlying bugs were non-deterministic enough that testing stale code sometimes returned clean-looking results by chance). Always run the full `npm run build` immediately before `npx wrangler deploy` for any worker change, and prefer verifying post-deploy against a distinctive value (e.g. checking `grep -c '<function-name>' dist/server/index.js` matches, or checking the actual persisted/returned value in production) rather than trusting a "Deployed" message alone.

## Superadmin console is now standalone (2026-07-23)

User supplied a high-fidelity design mock (design_handoff_superadmin/) and asked to implement it and take the superadmin console out of the customer-workspace sidebar entirely, as its own standalone dashboard.

**Routing change**: the old superadmin UI was inline React state (`adminMode`) rendered inside the same page as the customer Workspace — removed entirely, along with its component tree (`SuperadminDashboard`, `TemplatesPanel`, `PlanLimitsPanel`, `MessagePricingPanel`) and the `PLAN_OPTIONS`/`AdminWorkspaceSummary` types, all dead-code-deleted from `app/page.tsx`. It's replaced by a real, separate Next.js route: `app/admin/page.tsx` + `app/admin/admin.css`, statically exported to `/qpy-engage/admin/` (visible in `next build`'s route list and `out/admin/index.html`). The sidebar's "Superadmin" entry is now a plain `<a href="admin/">` (real navigation, not in-page state) — visible only to `isSuperadmin && !isImpersonating` accounts, same gating as before.

**Standalone auth**: `/admin` does its own bootstrap — reads `qpy-engage-auth-token` from localStorage (shared origin, same key the main app uses), calls `GET /api/auth/session`, and shows an honest access-denied screen (with a link back to the main app) if there's no token or `!user.isSuperadmin`, rather than assuming it was reached via the main app.

**Impersonation across a real page navigation**: the old design kept impersonation state in memory only (same page, same React tree), which doesn't work once the console is a separate page — a real page load throws away all component state. New one-time localStorage handoff: `/admin`'s "Impersonate" writes `{token, workspace, adminToken, adminUser}` to `qpy-engage-impersonation-handoff`, then navigates to the main app; `app/page.tsx`'s `Home` component reads + deletes that key on mount, and if present, adopts the impersonation token as the active session while stashing the superadmin's own token in memory (`adminSession`) for "Exit to Admin" to restore — same non-persisted-to-localStorage safety property as before (a refresh while impersonating still drops back to the superadmin's own session, not stuck impersonating). **Verified live**: impersonated a real customer (chinnnaapps), confirmed the "Viewing as chinnnaapps" banner + correct workspace rendered, clicked "Exit to Admin", confirmed it correctly restored "Smoke Test's workspace" with the Superadmin sidebar link back.

**New backend (worker/admin.ts)**: three new config surfaces to back the design's Plans/Integrations screens with **real, persisted, superadmin-editable** data instead of the mock's static numbers —
- `plan_pricing(plan, price_usd)` — real $ list price per plan, seeded (Free 0 / Starter 49 / Growth 149 / Scale 399 / Enterprise 1200). `GET/PATCH /api/admin/plan-pricing`.
- `plan_features(plan, feature, enabled)` — the 8 toggles from the design (whatsapp/instagram/webWidget/aiActions/ordersQpy/analytics/prioritySupport/whiteLabel), seeded per plan. `GET/PATCH /api/admin/plan-features`. **Honestly labeled as not yet enforced** — this configures what's nominally included per tier; nothing elsewhere in the app currently gates behavior on these flags (that's a real follow-up if feature-gating is wanted).
- `qpy_integration` (singleton row) — connected/apiKey-presence/channels/autoConfirmOrders/notifyEmail for the qpy.ai order-capture integration shown in the design. `GET/PATCH /api/admin/integrations/qpy`. **No real qpy.ai product exists in this codebase** — deliberately shows "Not connected" by default and lets a superadmin paste a real key if they have one, rather than fabricating a fake "Connected" status or a fake masked key (the design mock's static `sk_live_9f2a...` placeholder was NOT carried over).
- `listWorkspaces` extended with real per-workspace usage: `leadsCount` (from `action_submissions`), `whatsappMessageCount` (from `whatsapp_messages`), `messagesSentTotal` (from `workspace_message_sent`, see the per-category sent-counters work above) — replaces the design mock's fabricated "AI resolution %" stat, which has no real backing data anywhere in this app and was dropped rather than faked.

**Honesty deviations from the pixel mock** (the handoff doc explicitly says wire mock data to real APIs, not copy static numbers): Dashboard's "Churn rate" KPI (no real cancellation tracking) → replaced with "New this month" (real signup count). Analytics' "AI resolution rate" and "Avg response time" KPIs (untracked) → replaced with "Total WhatsApp messages" and "Total leads captured" (both real). The "MRR growth · last 8 months" bar chart (no historical revenue snapshots exist) → replaced with "Revenue by plan" (real: active customers × real list price per plan, computed live, not fabricated over time). Customer drawer's fabricated 3-invoice "Billing history" → dropped (no payment gateway integrated yet; showing invoice-looking rows with fake amounts would be a lie), replaced with the customer's real all-time message-sent total. Plans grid's single "Free WhatsApp msgs" field → replaced with all four real per-category `plan_limits` quick-edits (Marketing/Utility/Authentication/Service), since that's the actual real data model already built. "Seats" and "AI actions/mo" stats → dropped (no real per-seat or per-AI-action caps exist in this app for any plan).

**Verified**: all three new admin endpoints return real seeded data via a live authenticated call; toggling a plan feature in the UI round-tripped to the D1-backed value via a direct API check (flipped Free/webWidget off, confirmed via `GET /api/admin/plan-features`, then reverted and re-confirmed back to `true`); Dashboard/Customers/Plans/Automations/Integrations/Analytics views all screenshotted live against the real smoketest account's 5 real workspaces; customer drawer opens with real usage numbers; impersonate → exit round-trip verified as described above. No fabricated data was left in any view — every number shown is either read live from D1 or computed from live D1 data.

## Flows: predefined step-by-step conversations + Items catalog (2026-07-24)

User shared a screen recording of a real WhatsApp hotel-booking bot (fixed sequence: dates → nights → guests → rooms → phone → summary/confirm → a swipeable carousel of room options with title/price/image/"Book now") and asked for two conversation types — "with AI" (contextual, suggests from a catalog) and "without AI" (predefined, exactly like the recording) — sharing one Items catalog (name/title/price/image/external link). Scoped via AskUserQuestion to: a new standalone "Flows" nav section; build the Items catalog + predefined-flow builder first (AI-suggestion flow deferred); a fully generic step editor (any step type in any order, arbitrary branch targets) rather than a fixed set of step shapes.

**New Items catalog** (`worker/items.ts`): `catalog_items(id, workspace_id, name, title, description, price, currency, image_url, external_link)`. Full CRUD at `/api/items`. `getItemsByIds()` exported for the flow engine to resolve item ids into full records.

**New Flows engine** (`worker/flows.ts`) — the real generic step-graph runtime:
- `flows(id, workspace_id, name, trigger_text, status, start_step_id, steps_json)` — steps stored as a JSON array of `{id, type, prompt, variableName?, options?, itemIds?, next?}`. Step types: `message`, `buttons` (branching — each option has its own `next`), `text_input`/`number_input`/`date_input` (captures into a named variable, validates format), `yesno` (branching), `items` (renders a carousel from chosen catalog items), `summary` (supports `{{variableName}}` interpolation of everything collected so far), `end`.
- `flow_sessions(workspace_id, session_id, flow_id, current_step_id, variables_json)` — tracks where a live visitor is mid-flow, across turns.
- `runFlowForWidgetMessage()`: the core per-message engine call. If the visitor has an active flow session, validates their reply against the current step's expected input, stores it, resolves the branch target, then "plays forward" through any non-input steps (message/items/summary) — up to a 30-step-per-turn guard against a misconfigured self-referencing loop — until it hits another input-requiring step or the flow ends. If no session exists, checks whether the message contains any active flow's trigger phrase to start one. Returns `{handled:false}` when nothing matches, so the caller falls through to the normal AI assistant untouched.
- `POST /api/flows/test` — a **stateless** builder-side test endpoint: accepts the flow definition inline (so an unsaved draft can be tested) plus the current step/variables, and returns the next turn. This is what powers the dashboard's live "Test this flow" panel.

**Wired into the real Web widget** (`worker/widget.ts`'s `respond()`): before falling through to Claude, checks `runFlowForWidgetMessage()` first — a Flow (the "without AI" type) takes priority over the AI assistant for any visitor mid-flow or matching a trigger phrase. Flow-originated messages are stored into `widget_messages` (via a plain-text collapsed form for history) so Inbox/poll/refresh-resume all keep working unchanged. **WhatsApp is NOT wired to auto-trigger flows** — there's no automatic-reply pipeline for WhatsApp at all yet (Inbox is manual-reply only); Flows currently only run for real on the public Web widget, which is the one channel with a live automated-reply pipeline to hook into.

**Widget frontend** (`public/widget.js`): extended to render two new interactive message types beyond plain text — clickable button chips (clicking sends the label as the next chat message) and a horizontally-scrollable items carousel (image, title, price, a "View" button opening `externalLink` in a new tab). Backward compatible: the AI assistant path still returns a plain `reply` string; only flow turns use the new structured `messages` array.

**New dashboard section** (`app/page.tsx`): "Flows" added as its own top-level nav item (not nested under Automations) with two tabs — **Items catalog** (inline-editable table, no popups, matching this session's established editing pattern) and **Flows** (list with Draft/Active toggle + a step editor). The step editor is fully generic: every step has a type dropdown, and branching steps (Buttons/Yes-No) let each option target any other step via a dropdown — satisfying the "generic" scope decision over a fixed set of step shapes. A live **test-chat panel** sits beside the editor, calling `/api/flows/test` against the in-progress (possibly unsaved) flow so it can be verified before saving or activating.

**Honest scope notes**: the "AI understands context and suggests from the catalog" flow type was explicitly deferred to a future session (user chose predefined-flow-first). Feature toggles/qpy.ai-style gating aren't relevant here — nothing analogous. "See prices" (the recording's second button) was deliberately simplified to a single "View" action per item, per the user's own simplification ask (name/price/title/image/link only) — not a silent scope cut.

**Verified end-to-end** on a disposable throwaway account: created a real item, created a flow matching the recording's shape (buttons → date → nights → summary with `{{date}}`/`{{nights}}` interpolation → items carousel) via curl, then drove it through the *real* `/api/widget/respond` endpoint turn-by-turn — confirmed correct branching, correct variable interpolation in the summary, the real item rendered in the items carousel, and that once the flow ended, the very next message correctly fell through to the normal Claude-backed AI reply (proving the two conversation types coexist cleanly). Separately verified the invalid-input guardrail (a non-numeric reply to a number_input step is rejected with a helpful message and does not advance). Verified `/api/flows/test` statelessly against an unsaved draft. Live in the browser: created a real item through the inline UI, created a flow, changed its first step to Buttons and watched the editor regenerate the branching UI, and used the live test panel to click through — real buttons rendered from real (unsaved) step config. All disposable test data cleaned up afterward.

**UI refinement pass (same day, "it looks complex now"):** while building this, found and fixed a real completeness bug: `text_input`/`number_input`/`date_input` steps had no "Then go to" control in the UI at all — only `message`/`items`/`summary` did — so those three step types could be created but never actually linked forward to a next step, a silent dead end. Also found two mis-styled `<label>` elements missing the `full-label` class, which collapsed their caption text and input placeholder onto the same line with no separation. Fixed both, then did a broader density pass: removed the separate "First step" dropdown entirely (redundant — a step's position in the list already determines where the flow starts; reordering IS how you change it, one fewer control to hold in your head), added a colored left-accent bar per step card (blue for normal, purple for branching Buttons/Yes-No, gray for End) so step types are scannable at a glance, gave each step a numbered circular badge instead of plain "Step N" text, shrank the step-type `<select>` to a fixed width instead of stretching to fill the header, added a real "→" arrow between a branching option's label and its target step, and renamed verbose labels ("Prompt (message shown to the visitor)" → "Message", "Store answer as variable" → "Save reply as (optional)", "Next step" → "Then go to"). The Items catalog's dense 6-column always-editable table (image/name/title/price+currency-crammed-in-one-cell/link/actions) was replaced with a card list (thumbnail, name, price pill, title, link) plus a proper labeled two-column form for add/edit, matching the same field style as the flow editor instead of a spreadsheet. Verified live in the browser: the new item form renders as clearly labeled fields instead of a table row; switching a step's type to Number input now shows the previously-missing "Then go to" selector; switching to Buttons shows the purple-accented branching UI with the "→" arrows.

## Flows: auto-chaining + visual step insertion (2026-07-24, same day)

User: "creating the flows should be more dynamic and easy" — the previous version required manually wiring a "Then go to" dropdown on every single step, even purely linear ones, and steps could only be appended at the end.

**Engine change (worker/flows.ts)**: linear steps (message/question/items/summary) now chain to whichever step comes next in the builder's list **by default** — a new `arrayNext(steps, stepId)` helper computes "the following array item," used as the fallback wherever `step.next` isn't explicitly set. An explicit `step.next` (already-saved flows from before this change) still wins if present, so this is fully backward compatible — nothing already stored breaks. Branching (buttons/yes-no) is unaffected — those always need an explicit per-option target since that's the whole point of branching.

**UI change (app/page.tsx `FlowEditor`)**: the "Then go to" selector is gone entirely for linear steps — you just add steps in the order they happen, like writing a script. A step can now be inserted **at any position**, not just appended at the end — a faint "+" sits between every step (and at the very top/bottom); clicking it reveals a row of large tappable tiles (Message / Question / Buttons / Yes-No / Show items / Summary / End) instead of a `<select>` dropdown. Also consolidated `text_input`/`number_input`/`date_input` into one "Question" concept in the UI — inserting a Question step defaults to plain text, with a small pill toggle (Any text / Number / Date) to refine it after the fact, rather than three separate menu entries to choose between up front.

Verified: created a flow with zero explicit `next` values anywhere (just message → question → summary, in that order) and drove it through the real `/api/widget/respond` endpoint — confirmed it auto-advanced through every step correctly and the summary's `{{name}}` interpolation picked up the captured answer. Live in the browser: confirmed the insert-anywhere "+" opens the tile palette and correctly splices a new step into the middle of an existing flow, and that a fresh "Question" step shows the Any text/Number/Date toggle with no leftover "Then go to" control.

## AI-generated handoff summary for human agents (2026-07-24, same day)

User: "once the handover to the human agent is given, it should give a summary of previous chat what the agent should focus on and also feedback on customer anything to keep in mind."

**New `generateHandoffSummary()`** in worker/widget.ts: given a workspace+session, pulls the real conversation transcript (via the existing `getConversationHistory()`), asks Claude for a strict-JSON handoff brief — `{summary, focusOn, customerNotes}` — and stores it in a new `widget_conversation_state.handoff_summary` column (TEXT, JSON-encoded). Best-effort: any failure (Claude error, malformed JSON, etc.) is swallowed — a missing summary just means the agent reads the transcript themselves, it never blocks the actual handoff.

**Two trigger points**, both real handoff moments:
1. The AI's own `flag_for_human` tool firing (`flagConversationForHuman`) — the AI decided a human needs to take over.
2. A teammate manually clicking "Take over" in the dashboard (`setTakeover` with `active:false`) — regenerates the summary even if one already exists from an earlier AI flag, since more may have been said since.

**Surfaced in the dashboard** (`WebChatInbox` in app/page.tsx): `GET /api/widget/messages` now also returns `handoffSummary`; when a conversation is currently human-handled (`!aiActive`) and a summary exists, a highlighted purple card appears at the top of the conversation's side panel — "What's happened" / "Focus on" / "Keep in mind" — before the existing Lead details/Internal notes sections.

**Verified end-to-end via curl** against the real endpoints on a disposable account: sent a realistic frustrated-customer message ("order hasn't shipped, this is the second time I've asked"), triggered a manual takeover, and confirmed `GET /api/widget/messages` returned an accurate, well-written summary referencing the specific order and the customer's frustration. Separately verified the AI-escalation path (`flag_for_human` firing after a customer insists on a human) also produces a correct summary. One transient failure was observed and diagnosed with temporary logging (removed before the final deploy) — it turned out to be a one-off, not a logic bug; a second identical call succeeded, and two more full round-trips after redeploying the clean version both succeeded reliably. **Follow-up (same day): a real pre-existing crash was found and fixed while finishing this visual check.** Any brand-new signup crashed hard (Next's client error boundary, "This page couldn't load") the moment they opened the Inbox section — completely unrelated to the handoff-summary work itself, but discovered because that's exactly the page needed to see the new card. Root cause: `Workspace`'s `const selected = conversations.find(c => c.id === selectedId) ?? conversations[0];` (app/page.tsx) falls through to `undefined` when `conversations` (the legacy simulated-WhatsApp demo list) is empty — which it always is for a new account, ever since new signups stopped getting seeded demo data (see "clean defaults" work). `messages[selected.id]` is then read unconditionally on every render whenever Inbox is the active section, throwing `Cannot read properties of undefined (reading 'id')` for literally any fresh signup that had never once populated that legacy list — i.e., every real new customer. Fixed with a proper `EMPTY_CONVERSATION` fallback so `selected` is never undefined. Root-caused precisely (not guessed) by attaching a `window.addEventListener('error', …)` handler before triggering the section switch, since it's a client-side state change (no hard navigation) and the listener catches it live — far more reliable than screenshots for this class of bug. Verified fixed live: fresh account + real handoff-summary conversation → Inbox opens cleanly, conversation selects, and the "Handoff summary" card renders with accurate real content (order #4821, customer frustration, correct next steps) exactly as designed. This bug would have hit every single new customer's very first Inbox visit — worth flagging as the most impactful fix in this batch despite being unrelated to what was being built.

## Flows list restyled to match the superadmin design system (2026-07-25)

User pointed back at the design zip (`design_handoff_superadmin/`) asking to recheck it for "automations flow" design — after clarifying, they meant the customer-facing Flows list should visually match the zip's "Automation Templates" card design (the only automation-related screen in that file; it's actually a superadmin screen for deploying templates to customers, already built in `/admin` — a different feature from the customer Flows builder, which the zip has no dedicated design for).

Restyled the Flows list view (`app/page.tsx`'s `Flows` component) from a row-based `.action-list` to a 3-column card grid using the *exact* oklch tokens and layout from that section: icon chip (30×30, rounded 8px, light-indigo bg/indigo icon), a status pill top-right (Active = green, Draft = neutral gray), name, a description line, a "Deployed to X" analog (step count + decision-point count), an outlined "Edit flow →" button matching the mock's "Deploy →" style, and the black "+ New flow" CTA (already matched via the existing `.primary` class). Delete is a small hover-reveal × in the corner (not part of the original design, since flows don't have a multi-tenant deploy concept — this is a necessary addition, not a deviation). The step editor itself is unchanged — the zip has no flow-builder-with-steps screen to match against.

Verified live: created one Active and one Draft flow on a disposable account and confirmed the card grid renders with the pill colors, icon chip, and outlined buttons matching the design tokens exactly.

## Recommended next work

Done: real authentication, per-workspace data isolation, role-based access for team/WhatsApp management, real AI assistant replies (text + in-browser voice, with real voice selection, transcript toggle, max-duration auto-end, and interrupt/barge-in) via Anthropic Claude, real website knowledge ingestion (with real pasted-content sources for Document/FAQ, and a Browser Rendering fallback for JS-rendered sites — see below), a real public web chat widget with customizable icon/placement/effect/accent color (Channels > Web chat) plus a real configured assistant name/greeting shown in the widget header and "X is typing…" label, whose conversations are now persisted, viewable, and can be manually taken over by a human agent in Inbox > Web chat (AI pauses automatically once taken over, the visitor sees a system notice when handed off either direction, polling delivers the agent's replies back to the visitor's browser, with real typing indicators for both the AI and a typing human agent — dashboard also polls live so new visitor messages appear without a manual refresh, the AI automatically answers any message left unanswered when control is handed back to it, and automatically sends a brief holding message if a human hasn't replied within 5 minutes), real "AI Actions" execution (Claude tool-use actually calls the configured webhook, both in Test Studio and the public widget) with captured-lead storage that merges repeated captures within the same conversation into one progressively-completed record instead of duplicating (new "Leads" page), and an honest new-account experience — see "Authentication and multi-tenancy" and "AI assistant" above.

**Widget chat-window customization status:** icon (built-in or custom upload), launcher placement, launcher effect, accent color, assistant name, and greeting message are all real and configurable today (Channels > Web chat, plus Assistants > Profile for name/greeting). Not yet customizable: panel dimensions, fonts, header background color (separate from the accent color), or the "AI assistant"/"Type a message…" static label text — flagged as open follow-up, not yet built since scope wasn't confirmed.

**Widget session persistence:** `public/widget.js` keeps its `sessionId` in `sessionStorage` (per workspace), and a public `GET /api/widget/history` endpoint rebuilds the visible transcript on load — so a visitor refreshing the page (same tab) resumes their conversation instead of starting a new one. The session still resets if the visitor closes the tab or opens a new one (sessionStorage, not localStorage) — that's intentional, matching what was asked.

**Customer naming in Web chat inbox:** once a name is known for a session — either an AI Action explicitly capturing a `name`/`full_name`/`customer_name`/`first_name` field (`action_submissions`), or Claude passively noticing it in conversation via a built-in `record_customer_name` tool that's always available regardless of configured AI Actions (`callClaudeWithActions` in worker/shared.ts, stored on `widget_conversation_state.customer_name`) — `GET /api/widget/conversations` returns it as `customerName`, and the Web chat inbox list/detail header show it instead of "Website visitor". An explicit AI Action capture takes priority over the passively-learned name if both exist. Falls back to "Website visitor" until either fires.

**Internal notes on Web chat conversations:** Inbox > Web chat now has a notes panel beside the conversation (`widget_notes` table, `GET/POST /api/widget/notes`, session-authenticated) — team members can leave notes visible only to the dashboard (never the visitor), each tagged with the real author name/email from their session.

**Lead tagging (Source/Status/Priority/Segment):** the Leads page has a filter bar and per-lead dropdown editors for Source (Website/Referral/Event), Status (New/Contacted/Qualified/Nurture/Closed-Lost, defaults to "New" which also drives the "New" badge), Priority (Hot/Warm/Cold), and Segment (Enterprise/SMB) — new columns on `action_submissions`, updated via `PATCH /api/leads/:id` (worker/leads.ts), with server-side validation against the allowed values for each field. The same tags/badge are also surfaced in Inbox > Web chat: the conversation list shows the "New" badge next to a visitor's name when their associated lead's status is "New" (`GET /api/widget/conversations` joins lead status via `getLeadStatuses` in worker/widget.ts), and the conversation detail's right panel has a "Lead details" section (above Internal notes) showing the captured data plus the same four tag dropdowns, reusing `GET /api/leads?sessionId=` (new optional filter) and the existing `PATCH /api/leads/:id`.

**Superadmin dashboard (2026-07-19):** `users.is_superadmin` (default 0) marks a platform-operator account; currently only `smoketest+deploycheck@example.com` has it set. A superadmin sees a "🛡 Superadmin" sidebar entry leading to a real customer list (`GET /api/admin/workspaces`, worker/admin.ts) — every workspace with owner email, created date, WhatsApp connection status, and web chat conversation count. Clicking "Manage" calls `POST /api/admin/impersonate`, which mints a short-lived (4-hour) session token scoped to that customer's workspace (`sessions.via_admin`), and the frontend swaps into the exact same dashboard a customer sees — no separate/limited admin UI was built, since impersonation gives full access to everything already real (Assistants, AI Actions, Channels, working hours, Leads, Inbox, etc.). A persistent "Viewing as X · Exit to Admin" banner stays visible the whole time. The real superadmin token never touches localStorage during impersonation — only React state — so a page refresh always safely drops back out of impersonation rather than leaving it open. `loadSession()` gives a superadmin an Owner-equivalent role on any workspace they impersonate into without needing a real `workspace_members` row for every customer.

Verified end-to-end: confirmed a non-superadmin account gets a real 403 from `/api/admin/workspaces`; confirmed the customer list shows real signups (not just test accounts — found genuine independent customers `chinna@qpy.ai` and `arvind@123.com` who'd signed up on their own); impersonated the known-safe "Riley Coffee Co" throwaway test workspace end-to-end in the live browser (banner appeared, full real dashboard rendered scoped to that workspace's actual empty state, Exit to Admin correctly returned to the customer list) and via curl (confirmed the impersonation token could both read `/api/leads` and write `/api/state` for that workspace).

**How to apply:** If another account needs superadmin rights, set `is_superadmin=1` for that user directly in D1 (`UPDATE users SET is_superadmin=1 WHERE email='...'`) — there's deliberately no self-service UI for granting this, since it's a genuine cross-tenant privilege escalation. Impersonation sessions expire in 4 hours (vs. 30 days for normal logins) as a safety margin, separate from the "refresh exits impersonation" behavior.

**Superadmin: workspace status (enable/disable) and plan tier (2026-07-22):** `workspaces.status` (`'active'`/`'disabled'`, default `'active'`) and `workspaces.plan` (free-text label from a fixed set — Free/Starter/Growth/Scale/Enterprise, default `'Free'`) added as real, persisted columns (no fake billing system — `plan` is an honest manual label until real Stripe billing is ever built). The Superadmin dashboard's customer table now has Plan (dropdown, saves immediately via `PATCH /api/admin/workspaces/:id`) and Status (a toggle button) columns, both optimistic-UI with rollback on failure. Disabling a workspace is enforced in two places: `requireSession()` (worker/auth.ts) now returns 403 for a disabled workspace's own session, except when `session.isSuperadmin` is true (so a superadmin can still impersonate in to review/fix a disabled account); and the public widget's `respond()` endpoint (worker/widget.ts) returns 503 rather than generating any AI reply when the target workspace is disabled. `SessionContext` gained a `workspaceStatus` field, populated by `loadSession()`'s existing session query (added `w.status` to the join, no new query). Verified end-to-end against a disposable throwaway signup: confirmed the widget replied normally before disabling, confirmed disabling via direct D1 update immediately made both the widget (`503 "This chat is temporarily unavailable."`) and the owner's own `/api/auth/session` (`403` disabled message) reject, then deleted the throwaway workspace/user/sessions/messages afterward.

**Campaigns: real Edit, fixed Duplicate, more builder options (2026-07-22):** the campaign builder (browser-product functionality, per the section above — no real send/webhook backend) previously only stored display metadata per campaign (name/channel/audience/recipients/status/schedule/delivered/clicks) — the actual message text, image, CTA, URL, and schedule details were never persisted, so "Duplicate" silently produced a content-less clone and there was no Edit action at all. `Campaign` (app/page.tsx) now also stores `objective/message/mediaUrl/mediaName/cta/url/scheduleType/date/time/recurrence/excludeRecent`, so Duplicate and the new row-level Edit (pencil icon, hidden on already-Sent campaigns) both restore the real content into the builder. Also fixed "Save & exit," which previously discarded all progress silently — it now actually persists the in-progress campaign as a Draft (or updates it in place when editing). Added a few more builder options the user asked to consider: a "Send test to myself" button on the Message step, an "Exclude customers messaged in the last 24 hours" toggle on the Audience step (shaves the estimated recipient count), and a Repeat/recurrence dropdown (One-time/Daily/Weekly/Monthly) on the Schedule step.

**Real bug found and fixed during verification**: the three pre-existing real campaigns in the live "Smoke Test's workspace" predate this schema change, so they're missing the new fields entirely (`campaign.message` etc. are `undefined`). The first version of `edit()`/`duplicate()` copied those fields directly with no fallback, so editing (or editing a duplicate of) any of those three legacy campaigns crashed on `form.message.length`/`.trim()` the moment the wizard reached the Message step — reproduced live in the browser (twice) before being traced to this. Fixed by having both `edit()` and `duplicate()` backfill any missing field from a fresh empty-form default. Verified end-to-end live on the real account: duplicated "Summer collection launch" (a legacy Sent campaign with no stored message), confirmed the clone carried the real message text, edited the clone through to the Message step and confirmed it rendered with the correct pre-filled message and no crash, then deleted the test clone to leave the account clean.

**Why this matters:** any future schema addition to a `useStoredState`-backed type in this app must account for pre-existing real records that predate the new fields — don't assume `campaign.newField` is defined just because the type says it's required; backfill with defaults wherever existing data is read back into a form.

**WhatsApp message credits, pricing, and Marketing/Utility differentiation (2026-07-22):** user asked how customers could purchase WhatsApp messages and whether Qpy Engage could become a Meta "BSP" (Business Solution Provider / Tech Provider with Client Billing) to resell messaging. Explained the real mechanics (Meta bills whoever owns the Client Billing relationship — Qpy Engage would need Business Verification + Embedded Signup + Client Billing registration to bill on customers' behalf; the official "Meta Business Partner" badge is a separate, optional certification). User asked to build: a section for customers to buy credits, a superadmin option to manage message cost, and a dashboard option to differentiate Marketing vs Utility messages. **Explicitly scoped with the user first** (real charges vs. simulated top-up) — chose to simulate the purchase flow for now (matching the existing, already-labeled-simulated Subscription & Billing page) while keeping the pricing config, usage tracking, and category differentiation genuinely real.

Built:
- **`message_pricing` table** (worker/admin.ts) — real, persisted per-category USD price (Marketing/Utility/Authentication/Service), seeded with placeholder defaults (0.05/0.02/0.02/0) that are explicitly **not** claimed to match Meta's actual live rates (those vary by country/quarter) — copy in the UI says so directly. Superadmin-only `GET/PATCH /api/admin/message-pricing`; a customer-facing read-only `GET /api/pricing` (session-authenticated, any role) via the shared `listMessagePricing` export.
- **`workspace_credits` table** (new `worker/credits.ts`) — a real, persisted per-workspace USD balance. `GET /api/credits` (balance), `POST /api/credits/topup` (adds balance, response includes `demo:true`, capped at $5,000/topup), `POST /api/credits/deduct` (subtracts, clamped at 0).
- **Campaign builder**: added a real `messageCategory` field (Marketing/Utility — WhatsApp's actual billing taxonomy) to the Details step, and a real `estimatedCost` computed as `recipients × currently-configured price for that category` (fetched live from `/api/pricing`). Both fields persist on the `Campaign` record (extending the same type from the Edit/Duplicate fix above). Launching a campaign with status "Sent" calls `POST /api/credits/deduct` with the real estimated cost, so the workspace's credit balance genuinely reflects campaign activity (even though the underlying WhatsApp *send* itself is still simulated, per the existing Campaigns scope).
- **Campaigns list**: new Category column + Est. cost column per row, plus two real "Marketing spend" / "Utility spend" cards (summed from actual Sent campaigns' `estimatedCost`, grouped by category) — this is the literal "differentiate marketing vs utility" ask, backed by real math against real superadmin-configured pricing.
- **New "Credits" tab in Settings** (alongside the existing Billing tab) — shows the real current balance, a real spend-by-category table (same computation as the Campaigns cards, reusable), and an "Add credits" flow with fixed demo amounts ($10/25/50/100) that clearly labels itself as a demo top-up with no real charge, directly stating that real billing (Stripe and/or Meta Client Billing) isn't connected yet.
- **New "Message pricing" tab in the Superadmin dashboard** — lets a superadmin edit the per-category USD rate directly (blur-to-save inputs), which immediately affects every workspace's cost estimates and the customer-facing `/api/pricing` read.

**Verified end-to-end**: curl checks confirmed all new endpoints reject unauthenticated requests (401); created a disposable signup and confirmed the full customer flow (`GET /api/pricing` returned seeded defaults, topup/deduct correctly adjusted a real persisted balance); temporarily elevated that same disposable account to superadmin and confirmed `PATCH /api/admin/message-pricing` immediately changed what the customer-facing `/api/pricing` endpoint returned, and that an invalid category was rejected with 400; deleted all disposable test data afterward. Also verified live in the browser on the real `smoketest+deploycheck@example.com` account: created a real campaign (VIP customers, 1,248 recipients, Marketing category), confirmed the Review step showed the correct real math ($62.40 = 1,248 × $0.05), sent it and watched the Campaigns page's "Marketing spend" card update to $62.40 in real time, confirmed the Credits tab in Settings showed the same $62.40 real spend and a $0.00 balance, did a real demo top-up to $25.00 and watched the balance update, and confirmed the Superadmin "Message pricing" tab renders all four categories with editable rates. Cleaned up afterward: deleted the test campaign and reset the real account's demo credit balance back to $0 via direct D1 update, so production data reflects only what the user themselves has done.

**Why this matters:** this is a genuine "real infrastructure, simulated money-movement" pattern — the pricing config, usage math, and category tagging are all real and durable, but the actual charge/payment step is explicitly and visibly marked as a demo, consistent with how the pre-existing Subscription & Billing page already handles this project's stance on not fabricating fake financial transactions. Don't let a future session quietly wire the top-up button to look like a real charge without first integrating a real payment processor (Stripe) and/or completing Meta Business Verification + Client Billing registration — both are still fully unbuilt.

**How to apply:** if the user later wants real billing, the two prerequisites are (1) Meta Business Verification + Client Billing registration (to actually be billed by Meta for customers' usage), and (2) a real Stripe integration to actually charge customers for credits — neither exists yet. Until then, treat the credits/pricing feature as "real bookkeeping, demo payment step," and extend it the same way (e.g. adding Authentication/Service category tracking to real message paths) rather than building a parallel mechanism.

**Credits model changed from dollars to message counts (2026-07-22, later same day):** user asked to switch the credits model — instead of a single USD balance, customers purchase a *quantity of messages* per category (e.g. "buy 1,000 Marketing messages"), and sending a campaign consumes messages (not dollars) from that category's balance. Replaced `workspace_credits(balance_usd)` with a new `workspace_message_balance(workspace_id, category, message_count)` table (worker/credits.ts) — one row per category per workspace. `POST /api/credits/topup` now takes `{category, messages}`; `POST /api/credits/deduct` now takes `{category, messages}` and **genuinely rejects the deduction with a 402** if the workspace doesn't have enough messages in that category (returns the real shortfall in the error message) rather than silently clamping to zero like the old dollar-based version did. `GET /api/credits` returns all four categories' balances in one call.

The Campaign builder's Review step now shows "You have N {category} credits available" and, when sending immediately with an insufficient balance, shows a real inline warning ("Buy more in Settings → Credits") and **disables the Send button** — this is real enforcement, not just a display. The dollar cost estimate (from message pricing) is kept as informational context alongside the message count, not as the enforced resource. The Credits Settings page was reworked to a "Buy messages" flow: pick a category + a quantity (100/500/1,000/5,000 message packs), see the estimated dollar cost (informational, from the real superadmin-configured rate), and buy (demo, no real charge) — the resulting purchase adds to that category's real message balance. The page also shows real "messages sent this workspace" counts per category alongside the remaining balance.

**Verified end-to-end**: curl confirmed a deduct against a zero balance returns 402 with the exact real shortfall numbers, a topup then a partial deduct leaves the correct remainder, and an over-large second deduct against that remainder correctly 402s again. Live in the browser on the real `smoketest+deploycheck@example.com` account: bought 500 Marketing messages, tried to send a 1,248-recipient campaign (correctly blocked — Send button disabled, real warning shown with the exact 500-vs-1,248 numbers), topped up 1,000 more (1,500 total), same campaign then showed no warning and sent successfully, and the balance correctly dropped to 252 (1,500 − 1,248) afterward. Cleaned up the test campaign and reset the real account's message balance back to 0 via direct D1 update.

**Why this matters:** this is a materially different (and more realistic) prepaid model than the earlier dollar-balance version — the enforcement point is now a real "can't send more than you've bought" gate per category, not just informational spend tracking. Any future feature that consumes messages (e.g. real WhatsApp broadcast sending, once built) should call the same `POST /api/credits/deduct` endpoint and handle the 402 the same way (block, don't silently proceed).

**How to apply:** the dollar-based `workspace_credits` table and its old `/api/credits` `{balance}`/`{amountUsd}` shape are gone — anything referencing that old shape is stale. The current contract is `{balances: {Marketing, Utility, Authentication, Service}}` for reads, and `{category, messages}` for topup/deduct.

**Per-plan message limits, auto-granted to workspace balance (2026-07-23):** user asked for a per-plan message limit (Free through Enterprise), editable in Superadmin, that reflects on the customer dashboard — and specifically chose "auto-granted balance" over "informational only" when asked to clarify (auto-granted means the plan's included messages are actually added to the workspace's real, enforced message balance from the feature above, not just displayed as a number).

Built a new shared module `worker/messageBalance.ts` (to avoid a circular import between worker/admin.ts, worker/credits.ts, and worker/auth.ts, which all need this) holding: `MESSAGE_CATEGORIES`/`WORKSPACE_PLANS` constants, `plan_limits(plan, category, message_limit)` table (seeded with illustrative defaults per plan — Free through Enterprise, editable by superadmin), and `grantPlanMessages(db, workspaceId, plan)` — tops up each category's balance to at least the plan's limit, **never reduces** a balance already above it (so a real purchase on top of the plan's included quota is preserved). New superadmin endpoints `GET/PATCH /api/admin/plan-limits` and a "Plan limits" tab in the Superadmin dashboard (a 5-plan × 4-category editable grid). Grant is called from three places: `signup()` (new workspaces get their Free-plan quota immediately), `updateWorkspace()` in worker/admin.ts (changing a customer's plan tops up to the new plan's limits right away), and — importantly — `GET /api/credits` itself, so a workspace that predates this feature (or whose grant was otherwise missed) gets synced to its current plan's floor the next time anyone checks their balance, with no backfill migration needed. `workspaces.plan` is now also returned in `signup`/`login`/`sessionInfo` responses (`SessionContext` gained `workspacePlan`), and a new customer-facing `GET /api/plan-limits` returns the caller's own plan + its limits. The Credits Settings page now shows a "Plan includes X/mo" column next to the real balance, and a "— {plan} plan" label in the section header.

**Real bug hit and root-caused during verification, unrelated to this feature's own logic**: partway through this build, local `npm run build`/`tsc` began failing with `MODULE_NOT_FOUND` for `typescript`, and `git status` reported dozens of files "deleted" (package.json, tsconfig.json, next.config.ts, worker/meta.ts, db/schema.ts, README.md, etc.) that I had never touched. Root cause: the ephemeral scratchpad filesystem this session runs in had partially evicted files (likely a disk-pressure cleanup across a day/session boundary) — `.git/HEAD` itself was gone too (git initially reported "not a git repository" until that was restored: `echo "ref: refs/heads/agent/publish-github-pages" > .git/HEAD`). Recovered by: restoring `.git/HEAD`, then `git checkout -- <deleted paths>` to pull back most files from git's local object store (still intact), then for the handful of files whose git *objects* were also corrupted/missing (`.env.example`, `README.md`, `db/schema.ts`, `worker/meta.ts` — none of them files touched this session), doing a fresh throwaway `git clone` of the same branch from `https://github.com/mobileecommerce/qpy-engage` into a scratch directory and copying just those files across (safe, since the remote HEAD commit matched local HEAD exactly — nothing from this session had ever been committed/pushed, only deployed directly via wrangler/gh-pages). Finished with a clean `npm install` to rebuild `node_modules`. All of my actual in-progress edits (the "M" modified files) were untouched by any of this and were still exactly as I'd left them.

**Why this matters:** this environment's scratchpad storage is not guaranteed to persist untouched indefinitely — a future session hitting `MODULE_NOT_FOUND` or unexpected git "deleted" files mid-task should suspect the same filesystem-eviction cause before assuming a real code regression, and can use the same recovery playbook (restore `.git/HEAD` if missing, `git checkout` for anything git still has intact, a disposable fresh clone of the same branch for anything git's own object store lost, `npm install` to finish). This is an environment/tooling quirk, not a Qpy Engage product bug.

Verified end-to-end after recovery: curl confirmed a brand-new signup lands with the Free plan's exact quota (`{"balances":{"Marketing":100,"Utility":100,"Authentication":100,"Service":1000}}`); confirmed a superadmin plan-limit edit takes effect immediately and that changing a workspace's plan via the existing Plan dropdown re-grants to the new plan's (possibly just-edited) numbers; confirmed the customer-facing "Plan includes" column matches. Live in the browser on the real `smoketest+deploycheck@example.com` account (which predates this feature and had a stale $0/0-message balance left over from earlier testing that day): confirmed the Superadmin "Plan limits" grid renders and is editable, and confirmed that simply loading the Credits page synced the real account from 0 up to the Free plan's 100/100/100/1,000 — the on-read grant backfill working exactly as intended for a pre-existing account with no manual intervention.

**How to apply:** [[project-qpy-engage-status]] should be treated as current through this note. If the plan tiers or their limits change later, edit them via the Superadmin "Plan limits" tab — the underlying `plan_limits` table is the single source of truth for both the grant amounts and what customers see as "included."

**Real WhatsApp campaign sending + Audiences/Contacts management (2026-07-23):** user asked to make campaign sending hit the real WhatsApp API, and to add group/audience management (add/remove/edit contacts). Scoped explicitly with the user first: real campaign send uses an **already-approved Meta template** (no template-creation UI — that's Meta-side, requires their review), and a contact stores name + phone + consent + tags. Payment gateway is deliberately still not integrated (that's the user's stated "later").

- **Contacts/Audiences (`worker/contacts.ts`)** — three real per-workspace D1 tables: `contacts(name, phone, consent, tags)`, `audiences(name)`, and `audience_members(audience_id, contact_id)` join table. Full CRUD: `GET/POST /api/contacts`, `PATCH/DELETE /api/contacts/:id`, `POST /api/contacts/import` (CSV paste with name/phone header, optional target audience, marks imported as consented, capped at 5000 rows), `GET/POST /api/audiences`, `PATCH/DELETE /api/audiences/:id`, `GET/POST /api/audiences/:id/contacts` (list members / add existing), `DELETE /api/audiences/:id/contacts/:contactId` (remove from audience without deleting the contact). Audience list responses include real `memberCount` and `consentedCount` (only consented contacts are campaign-eligible). New "Audiences" sidebar section (`♟`) in app/page.tsx with a two-panel manager (audience list + contact table with inline consent-toggle/edit/remove/delete, add-contact form, add-existing dropdown, CSV import).
- **Real WhatsApp send (`worker/campaigns.ts`, `POST /api/campaigns/send`)** — reuses the exact existing Cloud API plumbing (exported `decryptToken`/`graphVersion`/`metaError`/`hmacHex`/`ConnectionRow` from worker/meta.ts): fetches the workspace's connected WABA, resolves the audience's **consented** contacts, and loops a real `type:"template"` message send to Meta's `/{phone_number_id}/messages` endpoint (same call shape as the already-live `sendTestMessage`). Deducts one message credit per **successfully sent** message from the campaign's category balance (via a new shared `deductMessageBalance()` in messageBalance.ts, which `credits.ts`'s deduct endpoint now also uses). Returns real sent/failed/deliveredPercent + up to 5 Meta error strings. Hard cap of 200 recipients per send (Workers wall-clock limit — larger sends need a real queue, which doesn't exist yet; the code says so honestly rather than silently truncating).
- **Campaign builder rewired**: the Audience step now lists the workspace's real audiences (with a "Go to Audiences →" link) instead of the old hardcoded segment names; recipient count = the selected audience's real consented count. The Message step gained "Meta template name" + "Template language code" fields (WhatsApp only) with an honest note that immediate WhatsApp sends call the real API with that template and that variable/component substitution isn't supported yet (so the template must work with no parameters — the composed message text is preview-only for WhatsApp, still used for Instagram/scheduled which remain simulated). The Review step blocks a real WhatsApp "Send now" (disabled button + inline warning) unless a real audience and a template name are set; on send it calls `/api/campaigns/send` and records the real delivered% on the campaign row.

**Deliberate scope boundaries (still simulated, by design):** Instagram campaign sending and any *scheduled* WhatsApp campaign are still simulated (no scheduler/queue infra exists) — only an immediate WhatsApp campaign with a template goes through the real API. Template creation/approval is Meta-side and not wrapped. Payment for buying extra message credits is still the demo top-up.

**Verified end-to-end** (without firing a real bulk send at numbers I don't control — that would be sending unsolicited WhatsApp messages): curl on a disposable throwaway account confirmed audience create → CSV-import 3 contacts → audience shows 3 consented → list members, and confirmed the send endpoint's guardrails all fire correctly (401 unauthed, 409 "connect WhatsApp first" when no WABA, 400 on missing template) — then deleted the throwaway account. Live in the browser on the real `smoketest+deploycheck@example.com` account: created a "Dubai VIPs" audience, added a contact through the form (audience updated to "1 consented / 1 total", row rendered with consent pill + tags), then in the Campaign builder confirmed the Audience step listed the real audience with its real consented count, the Message step showed the template fields, and the Review step correctly showed the real audience/credit numbers and **disabled the Send button with the "enter the Meta-approved template name" warning** — proving the real-send gate works without actually sending. The real-send code path itself is a near-identical copy of the already-verified `sendTestMessage` template call. Cleaned up: deleted the test audience/contacts via D1 and removed the leftover draft campaign from localStorage, leaving the real account exactly as before.

**How to apply:** [[project-qpy-engage-status]] should be treated as current through this note. To actually send a real WhatsApp campaign, a workspace needs (1) a connected WABA (Channels), (2) at least one Meta-approved template whose exact name is entered on the Message step, and (3) enough message credits in the campaign's category. If bulk sends beyond 200 recipients or scheduled/recurring real sends are ever needed, that requires a real background queue (Cloudflare Queues or a Durable Object + cron) — none exists yet, so the current cap and "immediate-only" real send are the honest limits. Payment-gateway integration for credit purchases remains the explicitly-deferred next item.

**Inbox slowness fixed + Audiences inline editing (2026-07-23):** user reported the Inbox took "very very long time" and asked that Audiences use inline editing instead of `window.prompt` popups.

- **Inbox perf (the big one):** `/api/widget/conversations` (polled by the dashboard Inbox every 4s) was taking ~2,460ms. Two causes, both fixed: (1) every widget endpoint called `ensureWidgetSchema()` which fired ~11 DDL round-trips including 3 always-throwing `ALTER TABLE`s *on every request* — now gated behind a module-level `widgetSchemaEnsured` flag so it runs once per Worker isolate, not per request (same pattern applied to `ensureContactsSchema` in contacts.ts and `ensureMessageBalanceSchema`/`ensurePlanLimitsSchema` in messageBalance.ts, since those are hit by the polled `/api/credits`); (2) `listConversations` ran ~5 D1 queries **sequentially** and D1 round-trips from this Worker are ~300–600ms each — the three that only depend on the session list (conversation-state, customer-names, lead-statuses) now run via `Promise.all`, and `getCustomerNames`'s own two lookups were parallelized too. Result verified live: `/api/widget/conversations` dropped from ~2,460ms to ~290ms (~8×). This is the general lesson for this codebase: **D1 round-trip latency dominates, so (a) never run per-request DDL that can be cached per-isolate, and (b) parallelize independent queries with `Promise.all` rather than awaiting them in sequence.**
- **Audiences inline editing:** replaced all three `window.prompt`/`window.confirm`-name flows in the Audiences section (app/page.tsx) with real inline UI — "Create audience" now reveals an inline name input row (Enter to save, Esc to cancel), "Rename" turns the audience header into an inline input, and a contact's edit (✎) turns that row's name/phone/tags cells into inline inputs with ✓/× save-cancel. (Deletes still use a `window.confirm` guard, which is appropriate for a destructive action — the user's complaint was specifically about *editing names/things* via popup, not destructive confirms.) Verified live on the real account: created an audience inline (no popup), confirmed the rename input appears pre-filled inline, deleted the test audience afterward — the user's own "Dubai VIPs" test audience was left untouched.

**How to apply:** the isolate-cache pattern (`let xSchemaEnsured = false;` early-return) is now the standard for every `ensure*Schema` in the Worker — a new one should follow it rather than running DDL on every call. Other list endpoints that do several sequential independent D1 reads (there may be more) are candidates for the same `Promise.all` treatment if they ever feel slow.

**WhatsApp Authentication (OTP) sending via per-workspace API keys (2026-07-23):** user asked how the "Authentication" message category (which existed only as a pricing/credit label) would actually be given to customers — via API? — and whether it was built. It wasn't, so built it. Authentication messages are WhatsApp OTP/verification-code messages, sent from the *customer's own backend* at the moment their system needs a code — so this is inherently a machine-to-machine API feature, not a dashboard-typed one. Qpy Engage is only the delivery pipe: the customer's system generates and validates the code.

- **`worker/otp.ts`** — a new `workspace_api_keys` table (id, workspace_id, name, `key_hash` (SHA-256, only the hash is stored), `key_prefix` (first 16 chars, for display), created/last-used timestamps). Dashboard-facing CRUD is session-authed and Owner/Admin-gated: `GET/POST /api/api-keys` (POST returns the full `qpy_live_…` key exactly once — it can never be shown again), `DELETE /api/api-keys/:id` (revoke). The machine-to-machine send endpoint `POST /api/whatsapp/send-otp` is authenticated purely by the API key (`Authorization: Bearer qpy_live_…` or `x-api-key`), not a session — deliberately skips the browser-origin allowlist since a customer's server has no Origin header and the secret key is the security boundary (same reasoning as the public widget endpoints). It reuses the exact Cloud API plumbing exported from meta.ts (`decryptToken`/`graphVersion`/`metaError`/`hmacHex`/`ConnectionRow`), sends a `type:"template"` message with the code as a body parameter plus (by default) a copy-code URL-button parameter (`copyCodeButton:false` omits it for body-only templates), and **reserves one Authentication credit up front via `deductMessageBalance`, refunding it if Meta rejects the send** so a failed send never silently consumes a credit and a successful one always does.
- **Frontend**: new "API & OTP" tab in Settings (added to every Settings sub-component's tab list) — shows the current Authentication credit balance (with a link to the Credits tab to buy more), the workspace's API keys (name + prefix + created + last-used, with revoke), an inline "create key" row that reveals the full key once in a copy-me green box, and a ready-to-paste `curl` example of the send-otp call.

**Verified end-to-end via curl on a disposable throwaway account**: unauthed send-otp → 401; created a key (returned full `qpy_live_…`), listed it (showed only the prefix, not the full key); send-otp with the valid key but no connected WABA → 409 **and confirmed the Authentication credit was NOT consumed** (the connection check runs before the deduction — balance stayed 100); missing-code → 400; invalid key → 401; revoked the key → immediately 401 on reuse and gone from the list. Threw the account away after. Live in the browser on the real smoketest account: the API & OTP tab renders with the real 100-credit balance and curl example, created a key and confirmed the one-time full-key reveal, then revoked it — leaving the real account with zero keys.

**Deliberately NOT built (the customer's responsibility / future work):** creating and getting Meta to approve the authentication *template* (Meta-side, can't be automated); code generation/validation (stays in the customer's system by design); and — as before — a real payment gateway for buying more Authentication credits (still the demo top-up). This turned the placeholder Authentication category into a genuinely working, credit-metered feature.

**How to apply:** a customer sends an OTP by (1) connecting their WABA, (2) creating an approved WhatsApp *authentication* template in Meta Business Manager, (3) generating an API key in Settings → API & OTP, and (4) having their backend `POST /api/whatsapp/send-otp` with `{to, code, templateName}` and the key as a Bearer token. Each successful send consumes one Authentication credit.

**Service messages now metered (2026-07-23):** the fourth category, Service (free-form WhatsApp replies inside the 24-hour customer-service window), was the only one with no credit consumption. Wired it: `sendInboxMessage` (worker/meta.ts — the real agent-typed WhatsApp reply, the only outbound Service send; the webhook only ingests, there's no AI auto-reply for WhatsApp) now decrements one Service credit after a successful Meta send, via a new **soft-meter** helper `meterMessageBalance()` (messageBalance.ts). Soft on purpose — it floors at 0, never blocks, and is `.catch()`-wrapped, so a credit balance or metering hiccup can never stop an agent from replying to a live customer. This deliberately differs from the *hard gate* on proactive sends (campaigns/OTP), because refusing a reactive support reply mid-conversation would be harmful and Meta itself doesn't gate service-window replies the way it gates templates. **Couldn't be end-to-end tested safely** — the only workspace with a real WABA is the live smoketest account whose active conversation is with a real person (+971585296252), and firing a test reply would message them — so verified by: typecheck + build + bundle-grep, code review (the helper is a trivial floor-decrement mirroring the already-verified `deductMessageBalance` minus the gate), the `.catch()` safety wrapper guaranteeing it can't break the reply, and a no-regression check that `/api/credits` still returns the Service balance. It'll visibly decrement the Service balance on the Credits page as real inbox replies are sent. **All four WhatsApp message categories are now wired to real send paths + credit consumption.**

**Real per-category "sent" counters (2026-07-23):** replaced the Credits page's "sent this workspace" column (and the Campaigns page's Marketing/Utility "messages sent" cards), which were derived client-side from localStorage campaign records (only counted campaign *recipients*, missed OTP + Service entirely). New `workspace_message_sent(workspace_id, category, sent_count)` table (created alongside the balance table in `ensureMessageBalanceSchema`) with `incrementSentCount()`/`getSentCounts()` in messageBalance.ts. Every real send path now increments its category's lifetime counter — best-effort, `.catch()`-wrapped so it never fails a send: campaigns.ts (by actual `sent` count), otp.ts (Authentication +1), meta.ts `sendInboxMessage` (Service +1, alongside the meter). `GET /api/credits` now returns `{balances, sent}`; the Credits page's balances table and the Campaigns cards both read the real `sent`. Verified: `/api/credits` returns the `sent` map (all zeros on a fresh account); inserted 42 Marketing + 7 Service directly and confirmed the endpoint surfaced them; live in the browser the Credits "Sent" column now reads the real server counter (0 for the smoketest account since no real sends have incremented it yet — the tell that the new code is live, since the old campaign-derived code would have shown 2,546 Marketing from the three demo campaigns). Disposable test data cleaned up. Now every real outbound WhatsApp message across all four categories both consumes a credit and increments an honest usage counter.

**Superadmin: platform automation template library (2026-07-22):** a new `automation_templates` D1 table (id, name, description, `action_json` — a full serialized `AssistantActionDef`, created/updated timestamps) lets a superadmin build one AI Action once and deploy it to any subset of customers (or all of them) instead of every customer building the same integration from scratch. New endpoints in worker/admin.ts, all superadmin-gated: `GET/POST /api/admin/templates`, `PATCH/DELETE /api/admin/templates/:id`, and `POST /api/admin/templates/:id/deploy` with body `{target: "all" | string[]}`. Deploy writes directly into the target workspace(s)' existing `qpy-engage-assistant-actions` workspace_state entry (the same key/format the customer's own Assistants > AI Actions UI reads and writes) — each deployed action is tagged with a `templateId` so re-deploying (e.g. after editing the template) replaces the previous copy in that workspace instead of duplicating it, while leaving the customer's own manually-created actions untouched. The webhook URL saved on a template is explicitly a placeholder ("each customer can edit this after deploy") since a shared template can't know a specific customer's real endpoint. The Superadmin dashboard gained a Customers/Automation templates tab switcher; the templates tab has a create/edit form (name, description, action name/type/description/method/webhook/default response) and, per template, an all-vs-specific-customers target selector (checkboxes over the real customer list) plus a Deploy button. Verified end-to-end twice: once via curl with a temporarily-elevated disposable superadmin account (create → list → deploy to a specific workspace → confirmed the action landed in that workspace's real `qpy-engage-assistant-actions` state → re-deployed the same template and confirmed it replaced rather than duplicated the entry → update → delete, all against a throwaway target workspace, everything cleaned up after), and again live in the browser using the real `smoketest+deploycheck@example.com` superadmin session (created a template, saw it render, switched to "Specific customers" and saw real checkboxes for every live customer, deleted it — dashboard back to empty state).

**Human escalation, business hours, and honest sidebar/header state (2026-07-19):**
- **Business hours**: Channels > Web chat has a "Business hours" editor (disabled by default) — per-day open/closed + start/end time, plus a timezone select. Stored via `qpy-engage-working-hours` workspace state. `computeBusinessHoursStatus()` in worker/widget.ts computes real open/closed status server-side at request time (in the business's own timezone) and injects it into the system prompt, so the AI can honestly say it's outside business hours instead of implying an instant reply.
- **Human escalation**: a built-in `flag_for_human` tool (worker/shared.ts, parallel to `record_customer_name`) that's always available regardless of configured AI Actions. System prompt instructs the AI to try to help first when a customer asks for a human, and only call the tool if they explicitly insist afterward — not a keyword trigger, an actual two-step conversational judgment call by the model. Sets `widget_conversation_state.needs_attention` + a reason, surfaced as a red "Needs you" badge in the Web chat inbox list and detail header; taking over a flagged conversation clears it (that's the acknowledgment).
- **Widget header subtitle**: previously hardcoded "AI assistant" even after a human took over. `GET /api/widget/poll`, `/history`, and the takeover response now report `aiActive`; `public/widget.js` updates the header subtitle to "Our team" once a human has the conversation.
- **Sidebar Inbox badge**: was hardcoded/derived from stale legacy mock data (always showed "4"). Now a real count of conversations (WhatsApp + Web chat) whose last message is from the customer, i.e. awaiting a reply — computed by lifting the existing summary-polling hooks up to the `Workspace` component.

Verified end-to-end: a throwaway workspace with working hours forced to always-closed correctly got the AI to mention being outside business hours on the first "can I speak to a human" ask while still trying to help, then correctly called `flag_for_human` only on the third clearly-insistent turn (not the first or second) — confirmed via the `needs_attention`/`attention_reason` columns and the `/api/widget/conversations` response. Header subtitle change confirmed live on the real qpy.ai widget (simulated a takeover via a direct DB write, sent a message, watched "AI assistant" become "Our team" in the same poll cycle). Sidebar badge confirmed showing a real "1" instead of the old hardcoded "4" on the smoketest workspace.

**Widget duplicate replies and Markdown leakage (fixed 2026-07-19):** two separate real bugs reported by the user from a live qpy.ai conversation. (1) The AI's reply could appear twice in the widget: a poll tick could land in the window between the server saving the reply and the in-flight `respond()` call receiving it client-side, so both paths appended the same message. Fixed in `public/widget.js`'s `pollForReplies` by re-checking each incoming message's timestamp against the *current* `lastSeenAt` at processing time (not the value the request was sent with), which catches the race regardless of which side resolves first. (2) Claude's replies sometimes contained raw Markdown (`**bold**`, `- bullet`, `1. numbered`) since this widget only renders plain text — those showed up as literal asterisks/dashes to the customer. The system prompt now explicitly forbids Markdown with a right/wrong example, but that alone wasn't consistently followed (especially for prompts that invite a list-style answer), so `sanitizeWidgetReply()` in `worker/widget.ts` strips bold/italic/code/list/heading markers server-side before a reply is ever stored or returned — a guaranteed backstop rather than relying on the model's compliance. Deliberately scoped to the widget only: WhatsApp has its own real `*bold*`/`_italic_` syntax that must never be touched by this. `public/widget.js`'s message CSS also gained `white-space:pre-line` so paragraph breaks the model does write render as actual line breaks instead of collapsing.

**AI Actions never expose webhook failures to the customer (fixed 2026-07-19):** when a "submit"-type action's configured webhook fails (wrong/unreachable endpoint, timeout, non-2xx), `callClaudeWithActions` (worker/shared.ts) used to feed the raw diagnostic text ("Could not reach the business system for this action...") into the tool_result Claude sees — the model then improvised on top of it, e.g. telling a real qpy.ai customer "I'm having a technical hiccup... please also email praveen@qpy.ai". Fixed: since a submit action's data is already saved locally as a safety net regardless of webhook outcome (see "Leads" below), a failed submit is now reported to the model as a clean success with an explicit instruction not to mention any system/process/issue — genuinely true from the customer's perspective, since their info really was captured. A failed "request"-type action (no local fallback data exists, e.g. "Check order status") still reports failure, but only the business's own configured `defaultResponse` message, with an explicit instruction to add no technical detail or invented workaround. `testAction()`'s full diagnostic text is untouched and still shown on the dashboard's "Test" button — this fix only changes what reaches the model in a live customer conversation. Verified with several repeated live calls against a deliberately broken endpoint; the qpy.ai workspace's "Capture qualified lead" action itself is still pointed at the leftover fake `api.atelierhome.com/leads` placeholder from before this account existed — that's a separate, still-open item (see below), independent of this messaging fix.

**Known dashboard bug fixed 2026-07-19:** persisting the active section (`qpy-engage-last-section::ws:<id>` in localStorage, so a refresh stays on the tab you were viewing instead of bouncing to Overview) initially caused a real crash on refresh when the restored section was Inbox — `useStoredState("qpy-engage-whatsapp-connected", false)` always starts at its `false` default before its async fetch resolves, and restoring straight into Inbox let it briefly render the legacy mock inbox against that default, then flip to `LiveInbox` mid-mount when the real value resolved, which crashed. Fixed by having `useStoredState` also return a `loaded` flag and only restoring the persisted section once `connected` has actually resolved (`Workspace` in app/page.tsx) — always boots into Overview first, silently switches to the last-viewed section a moment later. Reproduced and confirmed fixed via repeated real refreshes on the live site before shipping.

**Unified Inbox tabs:** `InboxHub` now shows a dynamic tab set instead of a fixed WhatsApp/Web chat switcher — a channel's tab (WhatsApp, Instagram, Web chat) only appears once it has at least one real conversation, and an "All" tab (a merged, recency-sorted list across channels, each row tagged by channel) only appears once 2+ channels have conversations. Instagram has no real messaging backend yet, so its count is always 0 and its tab stays hidden until that's built — deliberately not faked.

1. Add `ANTHROPIC_API_KEY` as a Cloudflare secret if it isn't set yet — without it the assistant endpoint returns a clear "not configured" error.
1a. **To fully fix knowledge ingestion for JavaScript-rendered websites (e.g. qpy.ai itself — a Vite/React SPA)**, set `CLOUDFLARE_BROWSER_RENDERING_TOKEN` as a Cloudflare Worker secret:
   - Create a Cloudflare API Token at https://dash.cloudflare.com/profile/api-tokens with **Account > Browser Rendering > Edit** permission (scoped to account `e8c99985a9f6a0ea4388241d964db68d`, already set as `CLOUDFLARE_ACCOUNT_ID` in `wrangler.jsonc`).
   - Run `npx wrangler secret put CLOUDFLARE_BROWSER_RENDERING_TOKEN` (name only — wrangler prompts separately for the value; never paste the raw token in chat).
   - Without this secret, website fetching safely falls back to its existing title/meta-description-only behavior — no breakage, just thinner content for SPAs until this is set.
   - Until then (or as a same-day alternative that works for any site), you can add real content immediately via **Knowledge > Add source > Document or FAQ**, which now has a real paste-content textarea — reliable regardless of how the source site is built, since it doesn't depend on fetching/rendering anything.
2. **New-account demo-content issue — fixed 2026-07-19:**
   - `useStoredState`'s localStorage cache was global to the browser, not scoped per workspace — signing into a new account on a browser that had used another account would leak that account's cached settings into the new one, and then write them back to its real server state. Now scoped by `${key}::ws:${workspaceId}`.
   - New workspaces no longer inherit the hardcoded "Atelier Home" demo persona (fake conversations/automations/knowledge sources, 2 sample AI actions pointing at a fake `api.atelierhome.com` domain, an assistant addressed as "Atelier Home's concierge"). Defaults are now derived from the real signed-up workspace name, and start empty otherwise. Overview's setup checklist and greeting now reflect real state (real user/workspace name, real knowledge-source count, real assistant-published flag) instead of hardcoded fake copy shown as complete for every account. Metric cards show a real conversation count and honestly mark resolution rate/response time/revenue as "—" (no analytics pipeline exists yet to back those) instead of inventing numbers. Verified live with a fresh throwaway signup (`riley.honesttest+*@example.com` — safe to delete, no real data).
   - The deeper Analytics page (chart bars, resolution-mix donut, per-automation completion %) still has hardcoded illustrative numbers — out of scope for this pass, would need a real analytics/reporting backend.
   - Any account created **before** this fix may have real server-side state already polluted with another workspace's cached settings — check with the user which account(s) this affected if they report stale data on an existing account.
2b. **Still open**: the real qpy.ai workspace's "Capture qualified lead" AI Action is still configured with the leftover fake `https://api.atelierhome.com/leads` endpoint (predates this account, from before the honest-defaults fix above). Every real submission still fails at the webhook step — captured leads are safe (see the Leads section above, and the tool-result sanitization fix, which stops the AI from telling the customer about this), but the business never gets an external notification until this is pointed at a real endpoint (Assistants > AI actions > Edit) or the webhook call is disabled for that action.
3. Rotate the exposed Meta App Secret, then update the Cloudflare secret; delete the now-unused `META_SETUP_KEY` secret.
4. Add explicit token metadata/health monitoring and an admin-only credential rotation screen.
5. Add password reset, email verification, and a workspace-switching UI (a user invited to a second workspace is only attached to it in the database today; login always resolves to their Owner workspace). Also add a way to rename a workspace / change an account's email — needed right now since the live "default" workspace is owned by a smoke-test account.
6. Convert Inbox polling to realtime delivery now that the authentication model is in place.
7. Add database-backed contacts, conversation assignment, resolution, notes, and message attachments (currently only WhatsApp message history is D1-backed; contact metadata is still per-browser).
8. If real inbound phone calls are wanted: set up a telephony provider account (e.g. Twilio), then build per-workspace phone number provisioning and webhook-driven call handling into the same Anthropic-backed assistant endpoint.
9. Implement real Instagram OAuth/webhooks, campaign template management, audience consent records, and scheduled delivery workers (bulk campaign "sends" are still simulated — Meta also requires approved message templates for outbound marketing sends outside the 24-hour customer-service window).
10. Complete Meta Business Verification, App Review, Advanced Access, and Embedded Signup production onboarding.
11. Wire up real billing (e.g. Stripe) if/when this needs to charge real customers; today's billing UI is fully simulated by design.
12. Build the "AI understands context and suggests from the catalog" flow type (deferred scope decision from the Flows feature above) — likely a new Claude tool (parallel to the existing AI Actions) that can browse the Items catalog and recommend/present items conversationally instead of following fixed steps.
13. Wire Flows to trigger on real WhatsApp inbound messages, once WhatsApp gets any kind of automated-reply pipeline (today WhatsApp Inbox is manual-reply-only — Flows currently only run for real on the Web widget, which is the one channel with a live automated pipeline).

## Definition of production readiness

Authenticated accounts and per-workspace data isolation now exist, but do not call the product production-ready until it also has: authorization audited on every customer-data endpoint, durable job processing, credential rotation, monitoring, backups, rate limiting, abuse controls, audit logs, password reset/email verification, and completed Meta approvals.
